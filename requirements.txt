# NOTE: 0.4.0 must be tagged/released from the 3phi-framework cleanup branch before
# this resolves from PyPI (the DAGs import StatLabeler, which 0.3.0 does not export).
# Until then, use the local-wheel dev flow (dev.env + FRAMEWORK_PATH).
# 3phi-framework==0.4.0
s3fs
pyarrow
tqdm
scikit-learn
lightgbm
typing-extensions
pandas
dask==2025.5.1
distributed==2025.5.1
fsspec
asyncpg
psycopg2-binary
apache-airflow-providers-celery
apache-airflow-providers-amazon
lz4
toolz==0.12.0
tornado
msgpack==1.1.1
xxhash==3.5.0
graphviz
Flask-AppBuilder
adlfs

# For StatLabeler
matplotlib
scipy
statsmodels
