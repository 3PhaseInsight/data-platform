# Framework dependency (set your local 3phi-framework repo path here)
FRAMEWORK_PATH=../3phi-framework

# data-platform-api
# Comma-separated list of accepted X-API-Key values
API_KEYS=
# Host port the API binds to (container port stays 8000)
API_PORT=8000